version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - .:/app
    command: sh -c "gunicorn 'app:app' --bind 0.0.0.0:5000 --workers 2 --threads 2 --timeout 120"
